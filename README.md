# Edge POS/WMS: Автономная система управления торговлей и складом

Этот проект представляет собой **edge-ориентированную платформу для управления торговлей (POS) и складом (WMS)**, спроектированную для автономной работы в условиях нестабильного или отсутствующего интернет-соединения. Система оптимизирована для развертывания на одноплатных компьютерах (SBC), таких как **Orange Pi 5**.

## 🚀 Ключевая идея

В основе системы лежит концепция **offline-first edge computing**:
- Все критически важные операции (продажи, складские перемещения) выполняются **локально** на edge-устройстве.
- Потеря интернет-соединения **не блокирует** основную деятельность.
- Взаимодействие с внешними сервисами (например, ОФД) происходит **асинхронно**.

Система представляет собой самодостаточный торгово-складской узел, который не зависит от облачной инфраструктуры для своей повседневной работы.

## 🏛️ Архитектура

Проект построен на основе **микросервисной архитектуры**, где все сервисы развернуты на одном физическом устройстве. Для изоляции и управления используются **systemd-сервисы**, что позволяет избежать накладных расходов на контейнеризацию в производственной среде.

### Основные сервисы:
- `api-gateway`: Единая точка входа для всех клиентских запросов.
- `auth-service`: Сервис аутентификации и авторизации (OAuth2).
- `catalog-service`: Управляет всеми справочными данными (товары, цены, склады, сотрудники).
- `pos-service`: Обрабатывает кассовые операции (продажи, возвраты, чеки).
- `warehouse-service`: Отвечает за складской учет (приемка, списание, перемещение).
- `search-service`: Обеспечивает полнотекстовый поиск по каталогу товаров с использованием Elasticsearch.
- `discovery-server`: Сервис обнаружения для микросервисной архитектуры.
- Вспомогательные сервисы для печати, синхронизации с ОФД и лицензирования.

Взаимодействие между сервисами осуществляется через **REST API** (для синхронных операций) и **Apache Kafka** (для асинхронных, событийно-ориентированных сценариев).

## 🛠️ Технологический стек

| Компонент | Технология |
|------------------|------------------------------------|
| **Бэкенд** | Java 17, Spring Boot, Spring Cloud |
| **Фронтенд** | Vue.js |
| **База данных** | PostgreSQL |
| **Брокер сообщений** | Apache Kafka |
| **Поисковый движок**| Elasticsearch |
| **Развертывание** | systemd на Linux (Armbian / Ubuntu) |

## 🏗️ Структура проекта

Проект организован как мультимодульный Maven-проект, где каждый микросервис является отдельным модулем:

- `api-gateway/`: API шлюз
- `auth-service/`: Сервис авторизации
- `catalog-service/`: Сервис каталогов
- `pos-service/`: Сервис кассовых операций
- `warehouse-service/`: Сервис складского учёта
- `search-service/`: Сервис поиска
- `discovery-server/`: Сервис обнаружения
- `common-lib/`: Общая библиотека с утилитами и моделями
- `frontend/`: Клиентское приложение на Vue.js
- `docs/`: Подробная проектная документация

## 📚 Документация

Более подробную информацию об архитектуре, сущностях базы данных и технических требованиях вы можете найти в папке [docs](./docs/).

---
*Этот README был сгенерирован на основе анализа внутренней документации проекта.*
